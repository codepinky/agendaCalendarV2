rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Bloquear todo acesso direto do cliente
    // O backend usa Service Account e ignora essas regras
    match /{document=**} {
      allow read, write: if false;
    }
  }
}






